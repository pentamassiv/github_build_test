













name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-on-aarch64:
    runs-on: ubuntu-latest
    name: ci (arm64)
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build in arm64 container
        uses: pentamassiv/run-on-arch-action
        with:
          arch: aarch64
          distro: ubuntu_devel
          # Install deps into the container. With the token, the container will be cached
          # The image is cached publically like a package
          githubToken: ${{ github.token }}
          #install: |
          #   rust-all libgtk-4-dev libadwaita-1-dev
          #   .. install packages for the arm container (e.g. Dockerfile steps)
          run: |
              uname -m
              cat /etc/lsb-release
          #   .. run build steps on the container
          #   cargo build --verbose
          #   cargo test --verbose