name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    #container:
    #  image: ubuntu:jammy@sha256:2166a543cffd7180cb98ed58bb6a99b0e4b57ecae8859c2a8ee5aa4f5e0a4fda
    #  options: --platform linux/arm64/v8

    steps:
    - name: Update system
      run: sudo apt-get update -y && sudo apt-get upgrade -y
    - name: Install QEMU
      run: sudo apt-get install qemu binfmt-support qemu-user-static
    - name: Check docker version
      run: docker version
    - name: Enable multiarch support for docker
      run: docker run --rm --privileged multiarch/qemu-user-static --reset -p yes
    - name: Create ARM container
      run: docker create -i -t --name buildContainer --platform linux/arm64/v8 ubuntu:jammy
    - name: Start ARM container
      run: docker start --attach -i buildContainer
    #- name: Attach to container
    #  run: docker attach buildContainer
    - name: Check architecture
      run: uname -m
    - name: Check release version
      run: cat /etc/lsb-release
    - name: Update system
      run: apt-get update -y && apt-get upgrade -y
    - name: Install Rust, GTK4 & libadwaita
      run: apt-get install -y rust-all libgtk-4-dev libadwaita-1-dev
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose